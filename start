apt-get update
apt-get install software-properties-common
apt-add-repository ppa:ansible/ansible
apt-get update
apt-get install ansible
ansible --version
vim /etc/ansible/ansible.cfg
  host_key_checking = false              (This is needed if you are adding ansible with terraform)
  
  mkdir terransible
  cd terransible
